{erl_opts, [debug_info]}.
{deps, [
    {jsx, "~> 3.0"}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [rtp]}
]}.


{relx,
    [{release, {rtp, "0.0.1"}, [rtp]},
        {mode, prod},
        {dev_mode, false},
        {include_erts, true}]}.
