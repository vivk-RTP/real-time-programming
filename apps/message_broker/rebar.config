{erl_opts, [debug_info]}.
{deps, [
    esq,
    {jsx, "~> 3.0"}
]}.

{shell, [
  % {config, "config/sys.config"},
    {apps, [message_broker]}
]}.

{relx,
    [{release, {message_broker, "0.0.1"}, [message_broker]},
        {mode, prod},
        {dev_mode, false},
        {include_erts, true}]}.